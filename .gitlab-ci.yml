stages:
  - linting

linting:
  stage: linting
  image: continuumio/miniconda3:latest
  script:
    - pip install git+https://github.com/psf/black
    - pip install isort
    - pip install flake8
    - black --check castor
    - isort castor --check-only
    - flake8 castor --ignore E501,F723,W503
